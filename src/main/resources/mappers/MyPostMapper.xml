<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allsparkstudio.zaixiyou.dao.PostMapper">
    <select id="countByAuthorIdAndType" parameterType="java.lang.Integer" resultType="java.lang.Integer">
        select count(*)
        from post
        where author_id = #{authorId,jdbcType=INTEGER}
        and type = #{type,jdbcType=INTEGER}
        and visible_outside_circle = 1
    </select>
    <select id="selectAllByTime" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from post
        where state = 0
        and visible_outside_circle = 1
        order by create_time desc
    </select>
    <select id="selectByUserIdAndType" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from post
        where state = 0
        and author_id = #{authorId,jdbcType=INTEGER}
        and type = #{type,jdbcType=INTEGER}
        and visible_outside_circle = 1
        order by create_time desc
    </select>
    <select id="selectPostsByCategoryId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from post
        where id in (select post_id from post_category where category_id = #{categoryId,jdbcType=INTEGER})
        and visible_outside_circle = 1
        order by create_time desc
    </select>
    <select id="selectPostsByCircleId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select *
        from post
        where id in (select post_id from post_circle where circle_id = #{circleId,jdbcType=INTEGER})
        and visible_outside_circle = 0
        order by create_time desc
    </select>
    <select id="selectFavoritesPostsByUserIdAndType" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select *
        from post
        where type = #{type,jdbcType=INTEGER}
        and id in (select post_id from user_post where user_id = #{userId,jdbcType=INTEGER} and favorited = 1)
        order by create_time desc
    </select>
</mapper>