<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allsparkstudio.zaixiyou.dao.CircleNoticeMapper">
    <select id="selectAll" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from circle_notice
        where circle_id = #{circleId,jdbcType=INTEGER}
        order by state desc, create_time desc
    </select>
    <select id="selectFirstNotice" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from circle_notice
        where circle_id = #{circleId,jdbcType=INTEGER}
        order by state desc, create_time desc
        limit 1
    </select>
    <select id="selectSecondNotice" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from circle_notice
        where circle_id = #{circleId,jdbcType=INTEGER}
        order by create_time desc, state desc
        limit 1
    </select>
</mapper>