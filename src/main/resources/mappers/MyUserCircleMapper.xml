<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allsparkstudio.zaixiyou.dao.UserCircleMapper">
    <select id="selectCircleIdsByUserId" parameterType="java.lang.Integer" resultType="Integer">
        select circle_id
        from user_circle
        where user_id = #{userId,jdbcType=INTEGER}
          and role &gt;= 2
    </select>
    <select id="selectMemberIdsByCircleId" parameterType="java.lang.Integer" resultType="Integer">
        select user_id
        from user_circle
        where circle_id = #{circleId,jdbcType=INTEGER}
          and role &gt;= 2
    </select>
    <select id="selectRoleOrNull" parameterType="java.lang.Integer" resultType="Integer">
        select role
        from user_circle
        where user_id = #{userId,jdbcType=INTEGER}
        and circle_id = #{circleId,jdbcType=INTEGER}
    </select>
    <select id="countMembers" parameterType="java.lang.Integer" resultType="java.lang.Integer">
        select count(*)
        from user_circle
        where circle_id = #{circleId,jdbcType=INTEGER}
          and role &gt;= 2
    </select>
    <select id="select5Members" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_circle
        where circle_id = #{userId,jdbcType=INTEGER}
        order by role
    </select>
    <update id="updateRole" parameterType="java.lang.Integer">
        replace into user_circle
        set user_id = #{userId,jdbcType=INTEGER},
            circle_id = #{circleId,jdbcType=INTEGER},
            role = #{role,jdbcType=INTEGER}
    </update>
    <select id="selectOwnerId" parameterType="java.lang.Integer" resultType="Integer">
        select user_id
        from user_circle
        where circle_id = #{circleId,jdbcType=INTEGER}
        and role = 4
    </select>
</mapper>